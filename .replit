run = "bash run.sh"

entrypoint = "server/index.js"
language = "nodejs"

[packager]
ignoredPaths = [".config", "node_modules"]

[deployment]
run = ["npm", "start"]
deploymentTarget = "auto"

[[ports]]
localPort = 3000
externalPort = 80

[nix]
packages = ["psmisc"]
